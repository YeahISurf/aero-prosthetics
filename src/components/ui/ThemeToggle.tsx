'use client';

// This component is now just a placeholder since we only use light mode
export default function ThemeToggle() {
  // Return an empty div to avoid breaking layouts where the toggle was used
  return <div className="w-10 h-10"></div>;
} 